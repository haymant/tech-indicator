---
name: reconcile-legacy-kb
description: Compare existing KB artifacts against the current legacy codebase and identify mismatches gaps stale assumptions and required KB updates.
argument-hint: A feature folder or legacy module to reconcile.
---

Read the feature artifacts and the corresponding legacy code. Use the code-verifier skill to identify mismatches, stale assumptions, and missing evidence. Propose the smallest KB updates needed to restore accuracy.